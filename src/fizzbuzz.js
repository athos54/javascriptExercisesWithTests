/**
 * Si el número es divisible entre 3 devolvera Fizz
 * Si el número es divisible entre 5 devolvera Buzz
 * Si el número es divisible entre 3 y 5 devolvera FizzBuzz
 * En cualquier otro caso devolvera el mismo número
 *
 * Tip: para saber el resto de una division se usa % ( 7 % 2 = 1 ó 26 % 3 = 2)
 */
export function fizzbuzz(number) {
  // aqui el codigo
}
